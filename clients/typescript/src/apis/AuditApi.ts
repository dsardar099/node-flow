/* tslint:disable */
/* eslint-disable */
/**
 * node-flow
 * Workflow orchestration. Definitions are declarative JSON; workers are language-agnostic and lease tasks from a queue.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';

export interface AuditListRequest {
    ns: string;
}

/**
 * 
 */
export class AuditApi extends runtime.BaseAPI {

    /**
     * Keyset-paginated: pass the previous page’s `nextCursor`. Offsets are deliberately not offered — a log grows at the head, so page two of an offset query silently skips whatever arrived while page one was read.
     * Read the audit log, newest first
     */
    async auditListRaw(requestParameters: AuditListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<any>> {
        if (requestParameters['ns'] == null) {
            throw new runtime.RequiredError(
                'ns',
                'Required parameter "ns" was null or undefined when calling auditList().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // apiKey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/ns/{ns}/audit`;
        urlPath = urlPath.replace(`{${"ns"}}`, encodeURIComponent(String(requestParameters['ns'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<any>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Keyset-paginated: pass the previous page’s `nextCursor`. Offsets are deliberately not offered — a log grows at the head, so page two of an offset query silently skips whatever arrived while page one was read.
     * Read the audit log, newest first
     */
    async auditList(requestParameters: AuditListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<any> {
        const response = await this.auditListRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
