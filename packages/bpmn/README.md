# bpmn

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bpmn` to build the library.

## Running unit tests

Run `nx test bpmn` to execute the unit tests via [Vitest](https://vitest.dev/).
